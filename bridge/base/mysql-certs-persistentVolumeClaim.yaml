#! mysql-certs-persistentVolumeClaim.yaml
# Generated code, do not edit
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: mysql-certs
    namespace: docker-compose-sql-php
    labels:
        com.docker.compose.service: mysql
        com.docker.compose.volume: certs
spec:
    accessModes:
        # TODO would need to check which services use this volume
        - ReadWriteOnce
    volumeMode: Filesystem
    resources:
        requests:
            storage: 100Mi
